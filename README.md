# Go Terminal App

This is a proof of concept to test the non-canonical input mode, plus some escape codes.

# Requirements

To run or build this program you will need *Golang* installed in your system.

# How to escape the program

The key combination "Control+C" is disabled; to escape the program, press "Escape" key.
